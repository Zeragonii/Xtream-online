# Contributing

This repository uses **Conventional Commits** because Release Please derives versions and changelog entries from commit messages.

Examples:

- `fix: stop orphaned ffmpeg sessions`
- `feat: add channel favourites`
- `feat!: change provider configuration format`

While the project is below 1.0, `feat:` bumps the minor version and `fix:` bumps the patch version.
